{
  "name": "@octoflash/extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome MV3 extension — queue YouTube videos into Octoflash with rendering options.",
  "scripts": {
    "icons": "node scripts/icons.mjs",
    "build": "node scripts/icons.mjs && node scripts/zip.mjs",
    "test": "echo \"no tests\" && exit 0"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
